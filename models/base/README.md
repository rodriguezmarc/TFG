Place the downloaded Stable Diffusion v1.4 checkpoint here.

Expected local path:
- `models/base/stable-diffusion-v1-4/`

If that folder exists and is non-empty, the pipeline will use it automatically.
Otherwise it falls back to the Hugging Face model id `CompVis/stable-diffusion-v1-4`.
